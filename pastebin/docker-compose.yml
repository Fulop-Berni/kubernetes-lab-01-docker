services:
  pastebin:
    build: .
    image: pastebin-python:local
    container_name: pastebin-python
    ports:
      - "8000:8080"
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request,sys; sys.exit(0) if urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=2).status==200 else sys.exit(1)"
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
